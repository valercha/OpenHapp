# OpenHapp packaging Makefile
#
# Scaffold for OpenWrt 25.12+ package metadata and build inputs.
# The first functional package will be a runtime daemon package,
# followed by a separate LuCI UI package.

PKG_NAME:=openhapp
PKG_VERSION:=0.1.0
PKG_RELEASE:=1

# These variables are placeholders for the real OpenWrt package build system.
# They will be replaced by package-specific metadata files and package.mk
# integration in the next packaging sprint.

.PHONY: all
all:
	@echo "OpenHapp packaging scaffold ready"
